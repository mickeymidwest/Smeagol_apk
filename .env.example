# Copy this file to .env and fill in your actual keys.
# .env should NOT be committed to git (it isn't tracked here, and
# self-improve's baseline commit only tracks files that already exist
# in the repo -- add a .gitignore entry for .env if you're putting this
# project under your own version control beyond Smeagol's own self-edit
# history).

ANTHROPIC_API_KEY=sk-ant-your-key-here
GEMINI_API_KEY=your-gemini-key-here
